# Claude Usage

Claude should call the same CLI commands documented in `agents/claude.md`.

Use stats commands for direct counts, search commands for evidence, and draft commands for reply preparation. Claude must not post directly or infer confirmation. Posting requires the explicit confirmation token produced for the exact draft shown to the operator.
